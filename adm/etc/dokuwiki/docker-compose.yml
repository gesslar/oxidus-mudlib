services:
  dokuwiki:
    build:
      context: .
      args:
        HOST_UID: ${HOST_UID:-1000}
        HOST_GID: ${HOST_GID:-1000}
    ports:
      - "8080:80"
    volumes:
      - ../../../doc/wiki:/var/www/html
    restart: unless-stopped
