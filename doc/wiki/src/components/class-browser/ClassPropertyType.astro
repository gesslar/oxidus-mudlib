---
interface Props {
  /** Type name */
  type: string;
  /** Syntax highlight type (maps to cb-hl-* CSS class) */
  highlight?: string;
}

const { type, highlight = "type" } = Astro.props;
---

<span class={`cb-hl cb-hl-${highlight} cb-type-annotation`}>{type}</span>
